FROM node:latest

ARG REDIS_ADAPTER_PORT
ARG REDIS_CONNECTION_URL
ENV REDIS_ADAPTER_PORT=$REDIS_ADAPTER_PORT REDIS_CONNECTION_URL=$REDIS_CONNECTION_URL

WORKDIR /app
COPY package*.json ./
RUN yarn
COPY ./ .

CMD [ "yarn", "start" ]